

## Plan: Host StackEval App Online

### What we'll do
1. **Copy the JSX file** into the project as the main page component
2. **Install `recharts`** dependency (already used by the file)
3. **Update the Index page** to render the StackEval app
4. **Add Google Fonts** (Inter + JetBrains Mono) to `index.html`

Once implemented, you can click **Publish** (top right) to make it live with a public URL.

